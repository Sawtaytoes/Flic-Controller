const base = global.baseDir

const configs = `${base}configs/`
const src = `${base}src/`

const fliclib = `${src}fliclib/`
const middleware = `${src}middleware/`
const services = `${src}services/`
const utils = `${src}utils/`

module.exports = {
	base,
	configs,
	fliclib,
	middleware,
	services,
	utils,
}
